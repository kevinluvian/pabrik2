apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mesin10.fullname" . }}-test-connection"
  labels:
    {{- include "mesin10.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mesin10.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
