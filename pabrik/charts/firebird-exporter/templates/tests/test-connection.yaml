apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "firebird-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "firebird-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "firebird-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
