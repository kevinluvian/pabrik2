apiVersion: v1
kind: Service
metadata:
  name: {{ include "firebird-exporter.fullname" . }}
  labels:
    {{- include "firebird-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "firebird-exporter.selectorLabels" . | nindent 4 }}
